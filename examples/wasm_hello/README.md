# wasm_hello

A minimal WASM proof for **boson-shell** — drives `BosonApplication` on `wasm32-unknown-unknown` and renders a static HTML payload to a canvas using the `vello-hybrid` (WebGL) renderer.
