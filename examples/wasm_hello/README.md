# wasm_hello

A minimal WASM proof for **strake-shell** — drives `StrakeApplication` on `wasm32-unknown-unknown` and renders a static HTML payload to a canvas using the `vello-hybrid` (WebGL) renderer.
